(A better way to type Unicode chars than by pecking out the code point with
`Ctrl+Shift+u`)

Now done through http://github.com/sharpsaw/unicode-dots
========================================================
